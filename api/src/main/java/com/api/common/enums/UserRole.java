package com.api.common.enums;

public enum UserRole {
  ADMIN,       // user who creates an account
  MEMBER,   // user who is invited
  GUEST, // user who is just joined a sprint (one time access)
}
